# My 8flyer 立て直し — HANDOFF

> **更新セッション**：`137dd9f3` ／ **2026-09-10 引き継ぎ作成**
> **計画の正本**：`~/.claude/plans/noble-swimming-blossom.md`（ユーザー承認済み）

---

## ① 何をしていたか

**My 8flyer（ANA特典航空券の旅程チェッカー）が一度も実利用されていない**ため、「旅程を判定する道具」から「**同じマイルでもう1都市を提案する道具**」へ作り直している。承認済み計画の **S-0・S-1・S-2・S-3・S-5 完了**（順序変更で S-5（UI）→ S-4（実便）の順・2026-09-06 ユーザー決定）。

**2026-09-11 時点：S-5（画面）は公開済み（最新の公開コミット `7be8042`）。S-4（実便）完了。S-6（特典の空き）は「提案に取れる日を出して並べ替える」まで実装済み。****いずれも未公開**（push していない・B層のため承認待ち）。

> 📄 画面設計 第6版＝実装後の記録：https://claude.ai/code/artifact/580d31c6-d0c5-42ea-aaaa-9f3b8b64578a

### 09-07（S-5 実装）

- 🔴 **【欠陥】「自宅で途中降機して、国内線1区間を国際線と別の日に飛ぶ」旅程を一覧から機械的に捨てていた。** 規約判定は通っていて候補も 2,752 通り生成されていたのに、同じ都市の中から**総飛行距離が最短の1本**だけを代表にしていたため、遠回りになる自宅経由が毎回消えていた（[proposer.js](src/proposer.js) の集約キーが `マイル|都市` だった）→ **キーに行き方を含める**形に是正
- **往路版も作った**（ユーザー指摘）。途中降機は往路・復路のどちらにも置ける。復路版＝帰ってからあとで飛ぶ／往路版＝先に飛んでおく
- **提案画面（さがす→候補→旅程）を実装**。[index.html](index.html) から proposer / miles-core を呼び、選んだ旅程を従来の判定UIへ引き渡す
- **旧 `MILE_CHART` を撤去**し、必要マイルを miles-core ＋公式チャートへ一本化。**旧表は14セル中8セルが公式と食い違っていた**（羽田→パリ ビジネス 旧140,000／公式115,000）

### 09-08（帰りの場所・ANA便だけの旅程）

- **帰りに降りる場所（帰着地）を検索時に指定できる**ようにした（`2a28f6e`）
- 🔴 **【欠陥】ANA便だけで組む旅程を「✅ スターアライアンス特典」として提案していた。** 実際には予約できない。**スタアラ特典には ANA以外の加盟社の便が1区間以上必要**（プロの実務解説2件で一致・ANA公式は製品名でしか書いていない）→ ANA便だけ＋日本発の途中降機は**提案から落とし**、途中降機なしは「ANA特典扱い」と印を付けてマイルをANAチャートで出す（`5473bd5`→`9906650`）
- **落としても候補は減らなかった**：エンジンが有効な別案（羽田→FRA[LH]→パリ）を選び直したため、🏠 はパリ・ホノルルとも5都市のまま

### 09-09（画面をいちどに1つ）

- ボタンが1画面に並んで押すべきものが埋もれていた → **さがす → 候補 → 旅程 → じぶんで組む** を1画面ずつ出す `ppGo()` ルータに（`f47e118`）
- 「先に飛ぶ」（往路の技）なのに復路の経路を表示していた不具合を是正

### 09-10（実利用からの4件・`7be8042`）

- **「もう1都市なし（まっすぐ帰る）」を選べる行として先頭に置いた**（寄り道でマイルがいくら増えるかを同じ画面で比べられる）
- **目的地・出発地の選択肢をゾーン・地域別に統一**（🌏 Zone N ／🇯🇵 グループ空港・個別空港）
- **じぶんで組む画面から逆に提案を受けられる**ようにした。置いてある経由地は「必ず通る街」（`mustVia`）として条件になる
- **Googleフライトのリンクに、その区間で特典に使える社を入れた**。アライアンス指定はGoogle側のURL内部形式（`tfs`）依存で壊れやすいため採らない（ユーザー判断）
- 検証：**単体 79/79・画面 48/48（従来UI 15＋提案フロー 33）PASS**、本番URLで5項目 PASS・JSエラーなし

### 09-10（S-4 実便の裏どり・取得系）

- **未決だった「Workers に置くか／ローカルで JSON を吐くか」を確定＝ローカル。** fast-flights の取得部は [primp](https://pypi.org/project/primp/) が **ChromeのTLS指紋を偽装**して Google を叩いており（`impersonate="chrome_145"`）、**Cloudflare Workers では動かせない**（native依存・データセンターIPは弾かれる）。実便は分単位の鮮度が要らないので、静的サイトに JSON を同梱する形が運用コスト0で優る
- **共有 .venv に入れられなかった。** fast-flights は `protobuf>=6.31`、共有 .venv は `google-generativeai`・`grpcio-status` が `protobuf<6.0` で固定。**専用venv `projects/my8flyer/.venv-flights`**（`.gitignore` 済み）。共有側に一度入れた3パッケージはアンインストール済み
- 🔴 **【欠陥】便の一覧が2か所に分かれており、半分近く取りこぼしていた。** Google の JSON は `payload[2]`＝おすすめのフライト／`payload[3]`＝その他のフライト の2枠を持つ。fast-flights は `payload[3]` しか読まない。**羽田→ロンドンは実在5便のうち2便しか取れず、ANA の NH211 が丸ごと落ちていた**のに `ok・2便` として正常終了していた → 両方を読むよう是正し、**payload の木を全部辿って取りこぼしを検出する検査**を追加（位置がまた増えても同じ形では再発しない）
- **1日だけのサンプルでは運航の有無を判定できないことが実測で判明。** 同じ区間でも日によって出たり出なかったりする（`VIE-HND` は5日中2日だけ・`HND-LHR` の NH211 も5日中3日）。→ **7日連続で取る**方針をユーザーが決定（2026-09-10）
- 検証：単体 **15/15 PASS**（うち8件が「形が変わったときに止まるか」）／実データで羽田→ロンドン **2便→5便**・羽田→フランクフルトは3便のまま

### 09-11（S-4 画面への接続・REQ-61）

- **取得を2段階にして 4.3時間 → 約1時間に短縮。** 1段目＝全295区間×1日／2段目＝**1日3本未満の区間だけ**×7日。
  実測で「3本以上の区間は7日とも運航」だったため、多頻度の214区間は曜日を調べない（追加1,770→486リクエスト）
- 🔴 **【欠陥】「0件」を「便が無い」と決めつけていた。** 羽田→ホノルルを `0本` と記録していたが、取り直すと6便あった
  （連続アクセスで一時的に弾かれると、空の結果が返る）→ **その区間で1本でも便を見たことがあるか**で分ける。
  見たことがなければ 15秒・45秒と間を空けて取り直す（**丁寧な確認は1区間1回**。毎日やると飛んでいない区間1本に7分かかる）
- 🔴 **【欠陥】観測していない曜日を「飛ばない」と表示していた。** 毎日8便の広島−羽田が「月曜だけ」に見えた。
  見た曜日の集合を**台帳全体**から取っていたのが原因（実際の観測範囲は区間ごとに違う）→ 区間ごとに計算し、
  7曜日そろわなければ**曜日を断定しない**
- **画面に実便を出した**（[index.html](index.html) の `renderRealFlights`）。便名・時刻・飛ぶ曜日。
  **運航会社で絞る**（`HIJ-HND` の JAL便7本は出さない）。隠した本数は明示する
- **REQ-61 green。** 旅程全体で①特典で乗れる実便が1本も無い区間 ②提携特典なのに実便の運航が全区間ANAだけ、を指摘する。
  実測：広島→羽田→パリは**全区間ANA運航**のため「ANA国際線特典として扱われる」と出る
- **基準日を 2026-11-09（月曜）に変更**（10/25 は冬ダイヤ初日＝切替の境目で便が載っていないことがある）。
  **TTL を7日→90日**へ（ダイヤはIATAの夏冬シーズンで決まり、シーズン中は変わらない。年4回で足りる）
- **2段階取得を完走**（1段目 295区間×1日＝25分／2段目 71区間×7日＝約60分）。`data/flights.json` は **295区間・2,239便・165KB**
  - **曜日運休のある区間が32本見つかった**（例：`VIE-HND NH206` 火金日／`IST-HND NH220` 月水土／`ARN-HND NH222` 火木日）。1日だけの観測なら全部「毎日」に見えていた
  - **直行便なしは 15区間 → 9区間**に減った（6区間は月曜に飛ばないだけだった）
- 検証：単体 **79/79**・実便パーサ **15/15**・画面 **59/59**（従来UI 15＋提案フロー 33＋実便 11）PASS

### 09-11（S-6 提案に「取れる日」を出す）

- **ANAの2画面の使い分けを設計として確定**（§22）。日本↔海外かつANA運航の区間だけが「面」で見られ、そこが律速。
  📄 読み物版：https://claude.ai/code/artifact/6d419296-a12e-404a-ac89-6a66fbff5715
- **ANA実画面3枚とも機械で読めることを実測**（カレンダー／前後3日比較／フライト一覧）。
  前後3日比較は `simpleCalendarDateGroup{Out|In}Bound{N}` ＋ `<em class="status">`、
  フライト一覧は `addOutboundSegmentInfoMap('0_0','NH','215','HND','CDG','X','20261121')`
- 🎯 **ANAの画面が 8flyer の計算を裏づけた。** ANA特典 55,000／スタアラ特典 62,000 が**2本のチャートと1マイルも違わず一致**。
  直行往復（本来 1-A＝55,000）がスタアラ特典では 62,000 からしか出ず、§20-6 の規則とも整合（ただし明文ではないので `verified_by` は据え置き）
- **カレンダーを畳んで [data/award-calendar.json](data/award-calendar.json)**（1区間＝180文字・18区間で4.4KB）
- **提案に「取れる日 N/180日」を出し、その順に並べる**。実測で**寄り道すると 7日 → 11日に増える**（広島→パリ）
- 🔴 **【欠陥】確認の深さが違うものを同じ物差しで並べていた。** 帰着地を変える案が軒並み「15/180日」で上位を占めたが、
  **往路しか確認できていない**だけだった（復路はカレンダーに載らない）→ ①面が無いと0日を区別 ②「行きだけN日（帰りは未確認）」と書く
  ③①往復とも確認→②片道だけ→③面が無い の順に束ねてから数で並べる
- **「もう1都市なし」は並び替えの対象にしない**（比べるための基準であって候補ではない・09-10 の決定を守る）
- **ビジネスの検索にエコノミーの表を流用しない**（`_meta.cabin` と一致しなければ出さない）
- 検証：単体 **79/79**・実便パーサ **15/15**・画面 **71/71**（従来UI 15＋提案フロー 33＋実便 11＋**取れる日 12**）PASS

### 09-12（規約の裏取り・一次情報化）

- 🎯 **「ANA運航便が0でもスターアライアンス特典は取れるか」を実画面で確定。取れる。**
  ユーザーが **フランクフルト→ニューヨーク**（ANA便が1便も飛ばない区間）で特典検索し、画面を保存。
  返ってきた9便（LH404 / LH986 / UA071 / SN2608 / SN501 / SN502 / SN2609 / UA014 / VL925）は
  **すべて ANA運航便フラグ false**。特典種別パネルには **ANA特典の列が無く、スターアライアンス特典のみ**。
  最低必要マイル 55,000マイル〜 が提示され「次へ」で予約へ進める状態だった
- `award-rules.json` に **`partner.carriers.star_no_ana_segment_required` = false** を新規登録（`verified_by: user+claude`）。
  `partner.carriers.star_alliance_combinable` の `verified_by` も `claude` → `user+claude` へ
- ⚠️ **上限側は未解決のまま。**「全区間ANAの旅程はスタアラ特典にならない」（`star_requires_non_ana_segment`）は
  依然として二次情報＋状況証拠（55,000/62,000 の整合）。公式の明文は見つかっていない

それ以前（S-2・S-3・第4版までの是正）の経緯は Vault `projects/my8flyer/architecture.md` §15〜§20 にある。

---

## ② 決定事項

| 日付 | 決定 |
|---|---|
| 09-02 | 本家「お得トラベル」の有料メンバーシップ（月1,290円）には**課金せず、自作・¥0** |
| 09-02 | 提案の入口は「**目的地を選ぶ → ルート提案**」／出発地は毎回選択／ANAへは**半自動**（人の操作ペース） |
| 09-03 | 実便データは **fast-flights（Google Flights）を採用** |
| 09-05 | 空席は**2段構え**＝面を特典カレンダー（1回で180日分）／点を空席検索で確定 |
| 09-06 | **保有マイルは設計に埋め込まない。**任意の入力欄でフィルタとしてだけ使う |
| 09-06 | アプリの本質は「同じマイルで、もう1都市」＝**寄り道1回をどこに使うかの提案** |
| 09-06 | **規約値が読めないときは判定を止める**（既定値で「問題ありません」を出さない）。fail-safe は必ず厳しい側へ |
| 09-07 | 「もう1都市」への行き方は **3通り**（🛬 帰着地変更／✈️ 寄り道／🏠 自宅で途中降機）。**一覧の行は都市ごと**にし、行き方は行の中に並べる |
| 09-07 | Q9＝**帰着地は就航8路線以上のまま**（現状維持）。この線引きは帰着地にだけ掛かり、寄り道には掛からない（松山＝5路線も ✈️ には出る） |
| 09-07 | Q10＝**海外は同じ国で1行に畳む／国内は都市ごとに残す** |
| 09-07 | Q5＝直せるのは**出発地（S2）と寄り道先（S3）**。座席クラス・特典の種類は S1 のまま |
| 09-07 | Q11＝**同じ国を1行に畳むのは海外だけ**。国内は都市ごとに残す（畳むと帰着地を選ぶ行為が消えるため） |
| 09-07 | Q12＝**「後日に飛ぶ」の上限は券の有効期間（旅行開始日から1年）**。途中降機に固有の日数上限は現行版に記載なし。海外側の滞在日数（ビザ）は本アプリの対象外 |
| 09-07 | **自宅で途中降機は往路・復路の両方で提案する**（ユーザー指摘。ANA公式の例示「往路・復路いずれか1回」で裏取り） |
| 09-08 | **帰りに降りる場所（帰着地）を検索時に指定できる**。指定した街は「もう1都市」に数えない（前提であって提案ではないため） |
| 09-08 | **帰りに出発する場所（海外オープンジョー）は提案画面から外し、目的地に固定**（ユーザー判断。提案段階では必須でなく、「じぶんで組む」で指定できる） |
| 09-08 | **「乗れる社がいる」と「実際にその便で組める」は別物として扱う。** 全区間で選べるのがANAだけの旅程は「ANA国際線特典」として扱う |
| 09-10 | **「もう1都市なし」も選べる行にする**／**選択肢はゾーン・地域別に統一**／**じぶんで組む画面から提案を受けられる（経由地も条件）**／**Googleフライトのリンクに乗れる社を入れる**（アライアンス指定はURL形式に依存するため採らない） |
| 09-10 | **S-4 の実便取得はローカルのバッチで行い、`data/flights.json` を静的サイトに同梱する**（Cloudflare Workers では fast-flights が動かないため。実便に分単位の鮮度は要らない） |
| 09-10 | **1区間あたり7日連続で取る**（1日だけでは便数の薄い路線を「直行便なし」と誤判定する。所要 約3時間／回・週1回まわす想定） |
| 09-10 | **スクレイパは「取れなかった」より「取れたつもりで中身が違う」を止める。** 取りこぼし検査で1本でも漏れたら `SHAPE_CHANGED` で停止し、書き出さない |
| 09-11 | **実便の取得は2段階。** 全区間×1日 → **1日3本未満の区間だけ**×7日（多頻度の路線に曜日運休は無い） |
| 09-11 | **「0件」は「便が無い」ではない。** その区間で1本でも便を見たことがなければ、間を空けて取り直す（弾かれと区別する） |
| 09-11 | **観測していない曜日を「飛ばない」と書かない。** 7曜日そろわなければ曜日を断定しない |
| 09-11 | **実便の表示は運航会社で絞る。** コードシェアの便名で「ANA以外の便がある」と数えない（REQ-61） |
| 09-11 | **空席の確認は区間ごとに道具を決める。** 日本↔海外かつANA運航＝カレンダー（面）／それ以外＝空席照会（点）。カレンダーで◎○が付いた区間は絞り込み段階で予約画面に入れない |
| 09-11 | **提案は「取れる日が多い順」を既定にする。**（面が効く検索のときだけ。切り替え可） |
| 09-12 | **スターアライアンス特典に ANA運航便は必須ではない**（下限なし）。ANA運航0の旅程（FRA→JFK・LH/UA/SN/VL）で成立することを実画面で確認＝**一次情報で確定**。上限側（全区間ANAは不可）は二次情報のまま |
| 09-11 | **確認の深さが違うものを同じ物差しで並べない。** 片道だけの確認は数字だけ出さず「帰りは未確認」と書き、往復確認できた行より下に置く |
| 09-09 | **画面はいちどに1つだけ出す**（さがす→候補→旅程→じぶんで組む）。ボタンが1画面に並ぶと押すべきものが埋もれるため |
| 09-08 | **スタアラ特典には ANA以外の加盟社の便が1区間以上必要**（実務解説2件で一致・公式は製品名でしか書いていない）。ANA便だけ＋日本発の途中降機は**提案から落とす**／途中降機なしは「ANA特典扱い」と印を付けマイルをANAチャートで出す |

---

## ③ 次の一手（最初の15分）

**S-6 の第一段（提案に「取れる日」を出す）まで実装済み・未公開。次は拡張の統合。**

**最初の15分でやること：**

1. `../../.venv/bin/python tests/browser_seats.py`（12件）で表示を確認する
2. **本番へ出す承認をユーザーから取る**（B層）。出すもの＝実便の表示・REQ-61・「取れる日」・
   `data/flights.json`（165KB）・`data/award-calendar.json`（4.4KB）
3. そのあと**拡張の統合**（§22-6）。予約用 `my8flyer-ana-bridge` に `cam.ana.co.jp` 権限が無く、
   カレンダーは調査用の別拡張（`spike-ana-calendar/`）が担当している。1つにまとめ、ボタンを3つにする
   - 📅 取れる日を見る … `<select id="zoneSelect">` と `<button data-class="X|R|I|O">` を拡張が合わせ、表が出てから撮る
   - 🎯 この日で1区間だけ確認 … 国際線1区間だけ入れて「前後3日の空席を比較」。結果の7日×2方向を読む
   - ✈️ 全区間を入力 … 既存機能

**ユーザーにお願いしている実測（まだ来ていない）：**
**ANA便が1便も飛んでいない区間**（例 フランクフルト→ニューヨーク）で特典検索を1回。
スタアラ特典の候補が返れば「ANA 0区間でも可」が一次の実測になり、
`award-rules.json` の `partner.carriers.star_alliance_combinable` の `verified_by` を更新できる。

デプロイの記録：

| 項目 | 内容 |
|---|---|
| 出したもの | 提案画面（さがす→候補→旅程）／自宅で途中降機（往路・復路）／陸路移動の導線／必要マイルの一本化／規約値4件<br>**2026-09-08（`2a28f6e`）**：帰りに降りる場所を検索時に指定できる<br>**2026-09-09（`5473bd5`→`9906650`→`f47e118`）**：ANA便だけの旅程の扱い（スタアラ特典にはANA以外の便が1区間以上必要）／画面をいちどに1つ／「先に飛ぶ」は往路を表示 <br>**2026-09-10（`7be8042`）**：「もう1都市なし」を選べる行に／選択肢をゾーン・地域別に統一／じぶんで組む画面から提案（経由地も条件）／GFリンクに乗れる社 |
| 出し先 | https://kreva605-design.github.io/my8flyer/ （GitHub Pages・`main` へ push） |
| 層 | **B層**（無認証で第三者に届く）。2026-09-07 にユーザーが公開を承認 |
| 巻き戻し | `git revert <コミット> && git push`（数十秒で戻る）。最新は `7be8042` |
| 出す前の確認 | 単体 **79/79**・画面 **48/48** PASS／秘密ファイルの混入なし／`data/snapshots/*.html` は `.gitignore` 済み |
| 出したあとの実測 | **2026-09-10 本番URLで5項目 PASS**：①選択肢がゾーン・地域別（🌏 Zone 2/3/4・🇯🇵 グループ空港/個別空港）②検索1.1秒・先頭行が「もう1都市なし（まっすぐ帰る）55,000マイル」③その行を選ぶと羽田→パリが出る④じぶんで組む画面から提案すると「フランクフルトを必ず通る」条件が効き全件がFRA経由⑤便確認カードのGoogleフライトリンクに乗れる社が入る（`flights from HND to FRA on Lufthansa or ANA` 等3本）。JSエラーなし |

**公開後にやること（未実施）：** 実機（スマホ）での目視（⑤の残作業 3番）。**Claude は実機を操作できないため、ここはユーザーの手が要る。**

> ⚠️ **このアプリは B層**（無認証で第三者に届く）。**push のたびにユーザー承認を取ること。** A層委任（確認なしでデプロイ）は当てはまらない（`~/.claude/docs/infra-inventory.md` §2.1）。

提案の中身を目で見るには：

```bash
cd projects/my8flyer
node tests/propose_cli.mjs HND CDG --stopover --top 14   # 🏠 は「自宅で途中降機」と表示される
node --test tests/*.test.mjs                              # 79件
../../.venv/bin/python tests/browser_propose.py           # 提案フロー 33件
../../.venv/bin/python tests/browser_smoke.py             # 従来UI・陸路・マイル・GFリンク 15件
```

---

## ④ 参照ファイル

| 種別 | パス |
|---|---|
| 計画の正本 | `~/.claude/plans/noble-swimming-blossom.md` |
| **判定ロジックの正本** | [src/rules-core.js](src/rules-core.js) |
| **必要マイルの正本** | [src/miles-core.js](src/miles-core.js)（1-A/1-B 判定・オープンジョー合算） |
| **提案エンジン** | [src/proposer.js](src/proposer.js)（`propose` / `groupByCity` / `homeStopover` / `soleCarrierOf`） |
| 距離（しんどさ） | [src/geo.js](src/geo.js)／[data/airports.json](data/airports.json)（OurAirports・public domain） |
| **乗れる航空会社の正本** | [data/airlines.json](data/airlines.json)（27社＋提携9社・**突合済み**）。取得＝`scripts/my8flyer/fetch_airlines.py`・**次回見直し 2027-03-06**・履歴は [data/airlines-history/](data/airlines-history/) |
| 提案の目視CLI | [tests/propose_cli.mjs](tests/propose_cli.mjs)（`node tests/propose_cli.mjs HIJ CDG --stopover`） |
| **実便の台帳** | [data/flights.json](data/flights.json)（区間→日付→便。**特典の空席ではない**・有償運賃の時刻表） |
| **実便の取得** | `scripts/my8flyer/collect_legs.mjs`（提案に出る区間を集める→`data/legs.json`）→ `scripts/my8flyer/fetch_flights.py`（Googleフライトから直行便を取る） |
| 実便の専用venv | `projects/my8flyer/.venv-flights`（fast-flights 3.1.0。共有 .venv とは protobuf が両立しない。`.gitignore` 済み） |
| 実便パーサの検査 | [tests/fetch_flights_test.py](tests/fetch_flights_test.py) **15件**（うち8件が「形が変わったら止まるか」） |
| 実便の集約 | `scripts/my8flyer/build_flights_index.py`（生台帳→「便×飛ぶ曜日」。835KB→163KB） |
| 実便の画面 | [index.html](index.html) の `renderRealFlights()` ／ REQ-61 は `renderFlightCard()` の `req61Note` |
| 実便の画面テスト | [tests/browser_flights.py](tests/browser_flights.py) **11件**（JAL便を出さない・NH211が出る・REQ-61） |
| **取れる日の台帳** | [data/award-calendar.json](data/award-calendar.json)（1区間＝180文字・18区間・4.4KB。**目安であって実在庫ではない**） |
| 取れる日の生成 | `scripts/my8flyer/build_award_calendar.py`（拡張のキャプチャ→畳む。空港コードに落とせない路線があれば終了コード2で止まる） |
| 取れる日の画面 | [index.html](index.html) の `ppSeatDays()` / `ppSeatLabel()` / `ppRender()` |
| 取れる日のテスト | [tests/browser_seats.py](tests/browser_seats.py) **12件**（深さの違うものを混ぜない・クラス流用しない） |
| 空席確認の設計（読み物） | https://claude.ai/code/artifact/6d419296-a12e-404a-ac89-6a66fbff5715 |
| 特典カレンダーの解析 | `scripts/my8flyer/parse_award_calendar.py`（拡張が保存したJSON→路線×日付×空席区分） |
| 特典カレンダーの調査用拡張 | [spike-ana-calendar/](spike-ana-calendar/)（読み取り専用。Chromeにインストール済み） |
| **規約値の正本** | [data/award-rules.json](data/award-rules.json)（**23値**・出典と原文つき・旧版併記） |
| 単体テスト | [tests/rules-core.test.mjs](tests/rules-core.test.mjs)＋[tests/proposer.test.mjs](tests/proposer.test.mjs)＝**計79件**（`node --test tests/*.test.mjs`） |
| ブラウザ疎通 | [tests/browser_smoke.py](tests/browser_smoke.py) 15件（従来UI・陸路・マイル・GFリンク）／[tests/browser_propose.py](tests/browser_propose.py) 33件（提案フロー）。どちらもポート8791 |
| マイルチャート | [data/mile-chart-partner.json](data/mile-chart-partner.json)（99ペア）／[data/mile-chart-ana.json](data/mile-chart-ana.json)（24ペア） |
| 取得スクリプト | `scripts/my8flyer/fetch_mile_charts.py`／`parse_award_calendar.py` |
| 空席スパイク拡張 | [spike-ana-calendar/](spike-ana-calendar/)（読み取り専用・調査用） |
| 再発防止基盤 | `scripts/scrape_guard/guard.py`／`~/.claude/commands/scrape-guard.md` |
| 設計書 | Vault `projects/my8flyer/architecture.md` **§15〜§20**（§20＝提案画面・自宅で途中降機・ANA便だけの旅程） |
| 公開ページ | https://kreva605-design.github.io/my8flyer/ |

---

## ⑤ 未解決・ハマりどころ

### 🔴 最重要のハマりどころ

**ANAの規約ページは1つのURLに新旧2版がタブで同居し、`aria-selected="true"` が付くのは古い版のほう。** 上から読むと旧版を拾う。**2026-09-06 にこれで2回続けて誤読した**。

→ **ANAの条文を引くときは必ず `/scrape-guard` を通し、`locator`（パネル名）を記録すること。**

### この作り直しで踏んだ罠（同じ穴を掘らないために）

| 罠 | 中身 | 教訓 |
|---|---|---|
| **畳むと最良案が消える** | 同じ都市の候補を「総飛行距離が最短の1本」に畳んでいたため、🏠 自宅で途中降機（＝わざと遠回りする案）が **2,752通り生成されていたのに毎回全滅**していた | 畳むキーには**利用者から見た種類**（行き方）を含める。畳む処理は必ず何かの基準で優劣を付けており、それが価値の軸とズレると最良案が静かに消える |
| **「乗れる社がいる」≠「その便で組める」** | 区間ごとに加盟社がいるかだけを見ていたため、全区間ANAの旅程を ✅ スタアラ特典として出していた（実際は予約不可） | 判定は**旅程全体**で持つ。区間ごとの OK を AND で束ねただけでは、全体に掛かる条件（ANA以外1区間以上）を取りこぼす |
| **`[hidden]` が CSS に負ける** | `.pp-form{display:flex}` が `[hidden]` に勝ち、隠したはずの画面が出続けた | `[hidden]{display:none!important}` を先に置く。表示切替は `el.hidden` で行う |
| **添字で行を選ぶと行の挿入で壊れる** | 先頭に「もう1都市なし」を差し込んだ瞬間、選択（`PP.sel`＝添字）が1つズレた | 選択は**都市キー**で持つ（`PP.selKey`）。並びが変わっても壊れない |
| **便の一覧が2か所に分かれている** | Google の JSON は `payload[2]`＝おすすめのフライト／`payload[3]`＝その他のフライト。`payload[3]` だけ読むと**羽田→ロンドンで5便中2便**しか取れないのに `ok` で返る | **件数ではなく中身を、知っている事実と突き合わせる**（「ANAは羽田−ロンドンを毎日飛ばしている」）。そのうえで**木を全部辿って取りこぼしを検出する検査**を機械側に置く |
| **1日だけのサンプルは運航の有無を語れない** | 同じ区間でも日で出たり出なかったりする（`VIE-HND` は5日中2日）。1日で「直行便なし」と書くと、便数の薄い路線を消す | 頻度を知りたいなら**連続した7日**を取る。1点の観測を「路線の性質」に格上げしない |
| **GoogleフライトのURLにアライアンス指定は載せられない** | 絞り込みは内部形式 `tfs`（base64のprotobuf）に入っており、手で組むと壊れる | 検索文（`q=`）に**実際に乗れる社名**を書く。仕様変更に強い |

### 確定した規約（現行版・2025-06-24以降）

| | ANA自社便 | 提携航空会社 |
|---|---|---|
| 国内乗り換え | 往路・復路**各1回** | 往路・復路**各1回** |
| 海外乗り換え | **0回**（規定なし） | 往路・復路各2回 |
| 途中降機 | **日本発は不可** | **旅程全体で1回** |

これらは [data/award-rules.json](data/award-rules.json) から読まれ、[tests/rules-core.test.mjs](tests/rules-core.test.mjs) の「現行版の規約値を採る（旧版を拾わない）」で固定されている。

### 残作業

残作業 5件 — **あなたの対応は3件（うち1件は任意）です**（内訳：1番=Claudeがやる 1件 / 2番=Claudeがやる（今は待ち） 1件 / 3番=あなたがやる 3件）。

| # | 判定 | やるか | 内容 | 放置するとどうなるか |
|---|---|---|---|---|
| 3 | `REQ-人手` | 👤 **あなたがやる** | **Chrome で本番URLを開き、ANA Bridge 拡張のボタンが有効になるか目視する。** 拡張は Chrome に v0.4.0 が unpacked で入っており有効（2026-09-10 に `Secure Preferences` を実測）。**Safari では原理的に動かない**（Chrome MV3 拡張のため） | ANAフォームへの自動入力が使えないままになる |
| 3 | `REQ-人手` | 👤 **あなたがやる** | **S-4 の画面を本番へ出す承認**（このアプリは B層＝push のたびに承認が要る）。出すもの＝実便の表示・REQ-61・`data/flights.json`（163KB） | 実便が手元にしか無い |
| 1 | `REQ-未達` | ✅ Claudeがやる | **S-6（特典の空き）。** ③「次の一手」の①〜⑤ | 「マイルは足りるが席が無い」ルートを上位に出し続ける |
| 3 | `REQ-人手` | 👤 **あなたがやる** | **（任意）ANA公式での裏取り：「ANA便だけの旅程はANA国際線特典として扱われる」。** 実務解説2件とご記憶で一致しており実装済みだが、出典が二次情報 | 二次情報のまま（判定は安全側なので実害は無い） |
| 2 | `REQ-待ち` | ⏸ Claudeがやる（今は待ち） | manifest の `sources.design.sha256` 確定。**解除条件＝TASK-DF-09 の完了**（`/verify` の参照欠損の誤検出） | 設計書を更新するたび `/verify` が同じ drift を出す |

> 実機（スマホ）目視は、S-4 の画面を出したあとにまとめて行うため、いったん上の「本番へ出す承認」に統合した。

### そのほか未解決（S-3 以降の判断材料）

| # | 内容 |
|---|---|
| 1 | ~~`ana` モードで周遊UIを出す是非~~ → **2026-09-08 に決着**。ANA便だけの旅程は途中降機なしなら「ANA特典扱い」の印を付けて出し、日本発の途中降機つきは落とす |
| 2 | 特典カレンダーの △（残席わずか）が1人で実際に取れるかは未検証。空席検索で確定するしかない |
| 3 | 提携社便（United等）の空席はカレンダーに出ない。seats.aero か空席検索でしか見えない |
| 4 | Jonty/airline-route-data は **2026-05-10 から17週連続で取得失敗**（上流の flightsfrom.com が403）。路線グラフは 2026-05-03 で凍結中。S-3 はこの凍結データで動かす |
