# My 8flyer 立て直し — HANDOFF

> **更新セッション**：`5f897ba5` ／ **2026-09-06 11:40**
> **計画の正本**：`~/.claude/plans/noble-swimming-blossom.md`（ユーザー承認済み・S-1 実施結果を追記済み）

---

## ① 何をしていたか

**My 8flyer（ANA特典航空券の旅程チェッカー）が一度も実利用されていない**ため、提案型へ作り直している。承認済み計画の **S-0（空席スパイク）と S-1（データ基盤）まで完了**、次は **S-2（rules-core の抽出）**。

途中で規約値の読み取り事故を起こしたため、**計画外だった `/scrape-guard`（案件横断の再発防止基盤）を先に作った**。

---

## ② 決定事項

| 日付 | 決定 |
|---|---|
| 09-02 | 本家「お得トラベル」の有料メンバーシップ（月1,290円）には**課金せず、自作・¥0** |
| 09-02 | 提案の入口は「**目的地を選ぶ → ルート提案**」／出発地は毎回選択／ANAへは**半自動**（人の操作ペース） |
| 09-03 | 実便データは **fast-flights（Google Flights）を採用**。`get ready to get banned` は Roadmap の未着手項目で現行実装の警告ではないと確認済み |
| 09-05 | 空席は**2段構え**＝面を特典カレンダー（1回で180日分）／点を空席検索で確定 |
| 09-06 | **保有マイルは設計に埋め込まない。**任意の入力欄でフィルタとしてだけ使う（汎用化のため） |
| 09-06 | アプリの本質は「同じマイルで、もう1都市」＝**寄り道1回をどこに使うかの提案** |

---

## ③ 次の一手（最初の15分）

**S-2：rules-core の抽出。** [index.html](index.html) の `validate()`（1555行〜）から `STATE`・`alert()`・DOM 依存を剥がし、`src/rules-core.js` に純粋関数として出す。

着手時に必ずやること：

1. **規約値は [data/award-rules.json](data/award-rules.json) から読む形にする。**現在 `index.html` の `AWARD_RULES`（1248行）にハードコードされており、**正本が2箇所にある状態**
2. 未実装の2条文を入れる（`award-rules.json` に `note` で記録済み）
   - 地上移動区間は両端の都市を合わせて1回と数える
   - 途中降機は乗り換え回数に含まれる
3. 回帰テストに「国内乗り換え2回は**不合格**」ケースを入れる（今回の誤りの再発防止）

---

## ④ 参照ファイル

| 種別 | パス |
|---|---|
| 計画の正本 | `~/.claude/plans/noble-swimming-blossom.md` |
| 規約値の正本 | `data/award-rules.json`（15値・出典と原文つき・旧版併記） |
| マイルチャート | `data/mile-chart-partner.json`（99ペア）／`data/mile-chart-ana.json`（24ペア） |
| 取得スクリプト | `scripts/my8flyer/fetch_mile_charts.py`／`parse_award_calendar.py` |
| 空席スパイク拡張 | `spike-ana-calendar/`（読み取り専用・調査用） |
| 空席の実測 | ユーザーの `~/Downloads/m8f-ana-calendar_20260905-*.json`（Zone2・Zone7） |
| 再発防止基盤 | `scripts/scrape_guard/guard.py`／`~/.claude/commands/scrape-guard.md` |
| 設計書 | Vault `projects/claude-org/architecture_2026-09-06_scrape-guard.md` |
| 公開ページ | https://claude.ai/code/artifact/ff701b94-a9aa-4947-87fa-7521bc820ac5 |

---

## ⑤ 未解決・ハマりどころ

### 🔴 最重要のハマりどころ

**ANAの規約ページは1つのURLに新旧2版がタブで同居し、`aria-selected="true"` が付くのは古い版のほう。** 上から読むと旧版を拾う。**2026-09-06 にこれで2回続けて誤読した**（ユーザーの指摘で発覚）。

→ **ANAの条文を引くときは必ず `/scrape-guard` を通し、`locator`（パネル名）を記録すること。**

### 確定した規約（現行版・2025-06-24以降）

| | ANA自社便 | 提携航空会社 |
|---|---|---|
| 国内乗り換え | 往路・復路**各1回** | 往路・復路**各1回** |
| 海外乗り換え | **0回**（規定なし） | 往路・復路各2回 |
| 途中降機 | **日本発は不可** | **旅程全体で1回** |

**アプリの現行設定は4項目とも正しい。**（一度「誤り」と報告したが撤回済み）

### 未解決

| # | 内容 |
|---|---|
| 1 | **正本が2箇所にある**（award-rules.json と index.html）。S-2 で解消 |
| 2 | ANA自社便は日本発の途中降機ができないため、**8flyer の周遊は実質 提携特典専用**の可能性。`ana` モードで周遊UIを出す是非を S-2 で実機確認する（推論段階・断定していない） |
| 3 | 特典カレンダーの △（残席わずか）が1人で実際に取れるかは未検証。空席検索で確定するしかない |
| 4 | 提携社便（United等）の空席はカレンダーに出ない。seats.aero か空席検索でしか見えない |
| 5 | 都市マスタが66都市しかなく、経由候補7都市が「未登録」で落ちた（SFO・EWR・IAD・IAH・AKL・YYZ・MAJ）。Wikipedia で拡充する |
| 6 | Jonty/airline-route-data は **2026-05-10 から17週連続で取得失敗**（上流の flightsfrom.com が403）。路線グラフは 2026-05-03 で凍結中 |
